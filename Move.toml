[package]
name = "arbitrage_system"
edition = "2024.beta"
license = "MIT"
# authors = ["Mahmoud Mourad <mahmoud.mourad.dev@gmail.com>"]

[dependencies]
Sui = { git = "https://github.com/MystenLabs/sui.git", subdir = "crates/sui-framework/packages/sui-framework", rev = "framework/testnet" }

# AMM integrations (uncomment + pin the published rev/address per network when
# wiring the live adapters in sources/adapters/). Keeping them out of the default
# build means `sui move build` stays fast and dependency-free for the executor.
#
# CetusClmm = { git = "https://github.com/CetusProtocol/cetus-clmm-interface.git", subdir = "sui/clmmpool", rev = "<pin>" }
# Turbos    = { git = "https://github.com/turbos-finance/turbos-sui-move-interface.git", subdir = "...", rev = "<pin>" }

[addresses]
arbitrage_system = "0x0"

[dev-dependencies]

[dev-addresses]
